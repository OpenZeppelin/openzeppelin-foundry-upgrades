---
'@openzeppelin/foundry-upgrades': minor
---

Add support for Hardhat 3 compatibility, including enhanced build info directory handling and artifact path resolution. The library now automatically detects and uses Hardhat 3's build-info directory structure (`artifacts/build-info`) when available, and properly handles Hardhat 3 artifact format with canonical path names.
